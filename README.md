## Port Scanner (Python)

A simple Python script to scan TCP ports on a target IP using multithreading.

## Features

- Multi-port scanning via command-line
- Fast execution with threading
- Basic IP and port format validation

## Usage

```bash
python3 scanner.py -t <target_ip> -p <port1,port2,...>
